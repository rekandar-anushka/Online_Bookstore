<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{#if isAdminLogin}}Admin Login - Online Bookstore{{else}}Login - Online Bookstore{{/if}}</title>
  <link href="/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body>
  <div class="container mt-5">
    <h2 class="mb-4">{{#if isAdminLogin}}Admin Login{{else}}User Login{{/if}}</h2>

    {{#if isAdminLogin}}
      <div class="alert alert-warning text-center">
        You are logging in as an administrator.
      </div>
    {{/if}}

    {{#if error}}
      <div class="alert alert-danger">
        {{error}}
      </div>
    {{/if}}

    <form action="{{#if isAdminLogin}}/admin/login{{else}}/login{{/if}}" method="POST" class="w-50 mx-auto">
      <div class="mb-3">
        <label for="username" class="form-label">Username</label>
        <input required type="text" class="form-control" id="username" name="username" placeholder="Enter username" />
      </div>
      <div class="mb-3">
        <label for="password" class="form-label">Password</label>
        <input required type="password" class="form-control" id="password" name="password" placeholder="Enter password" />
      </div>
      <button type="submit" class="btn btn-primary">Login</button>

      {{#unless isAdminLogin}}
        <p class="mt-3">
          Don't have an account? <a href="/register">Register here</a>
        </p>
      {{/unless}}
    </form>
  </div>

  <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>
